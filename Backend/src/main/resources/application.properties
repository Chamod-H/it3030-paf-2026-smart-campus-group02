# ── Server Configuration ──
server.port=5000

# ── MongoDB Database Connection ──
# Note: In standard MongoDB Atlas URIs, the target database name (campus_ms) 
# must go before the '?' query parameters for Spring Boot to detect it correctly.
spring.mongodb.uri=mongodb+srv://chamod:c123@cluster0.rbyoxdu.mongodb.net/campus_ms?appName=Cluster0
# Optional: Explicitly define the database name if you ever switch to a localhost URI
spring.mongodb.database=campus_ms

# ── Logging ──
# Optional: Set this to DEBUG if you want to see the exact queries Spring is making
logging.level.org.springframework.data.mongodb.core.MongoTemplate=INFO

# ── JWT Security ──
# Fixed 256-bit Base64 secret key - NEVER regenerate this after deployment
jwt.secret=c21hcnRDYW1wdXNTeXN0ZW1TZWNyZXRLZXkyMDI2U2xpaXRLbmQwMlN5c3RlbQ==
jwt.expiration=36000000

# ── Error Debugging ──
server.error.include-message=always
server.error.include-binding-errors=always

# ── Mail Server Configuration ──
# To enable real Forgot Password emails, provide a valid Gmail address and App Password here
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pasinduh84@gmail.com
spring.mail.password=urat osrg fwjf jpne
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
